<?xml version="1.0" encoding="utf-8"?>

<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@drawable/card_background"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="120dp">

  <org.devconmyanmar.apps.devcon.ui.widget.ForegroundImageView
      android:id="@+id/workshop_background"
      style="@style/KeynoteView.KeynoteImageStyle"
      android:src="@drawable/dc14"
      android:foreground="@drawable/image_overlay"
      android:contentDescription="@string/keynote_background"
      android:layout_width="match_parent"
      android:layout_height="match_parent"/>

  <LinearLayout
      android:orientation="vertical"
      android:background="@drawable/gradient_overlay"
      android:layout_gravity="bottom|start"
      android:layout_width="match_parent"
      android:layout_height="wrap_content">

    <TextView
        android:id="@+id/workshop_title"
        style="@style/KeynoteView.KeynoteTitleStyle"
        tools:text="Android Workshop"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <TextView
        android:id="@+id/workshop_time_and_place"
        style="@style/KeynoteView.KeynoteTimeAndPlace"
        tools:text="@string/talk_detail_time_and_place"
        android:background="@drawable/gradient_overlay"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

  </LinearLayout>
</FrameLayout>